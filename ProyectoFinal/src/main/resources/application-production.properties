spring.application.name=gestion-tareas

# Conexión a MongoDB Atlas usando variable de entorno
# En Railway, configura la variable: MONGODB_URI
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://deivid444333666_db_user:IySUHAyArVmN3goQ@gestiontareas.fpxvzuc.mongodb.net/gestion_tareas?appName=GestionTareas}

# Configuración del servidor
server.port=${PORT:8080}

# Configuración de logs para producción
logging.level.root=INFO
logging.level.com.gestionproyectos=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Desactivar DevTools en producción
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false

# Configuración de Thymeleaf para producción
spring.thymeleaf.cache=true

# Configuración de seguridad
# En Railway, puedes agregar esta variable para mayor seguridad
# JWT_SECRET=tu-secret-key-aqui-muy-larga-y-segura

# Habilitar compresión de respuestas HTTP
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
server.compression.min-response-size=1024
